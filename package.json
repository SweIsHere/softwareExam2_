{
  "name": "cs-grade-calculator",
  "version": "1.0.0",
  "description": "CLI tool to calculate final grades for UTEC students.",
  "type": "module",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "clean": "rimraf dist",
    "start": "tsx src/index.ts",
    "start:prod": "node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.7.5",
    "rimraf": "^6.0.1",
    "tsx": "^4.17.0",
    "typescript": "^5.6.3",
    "vitest": "^1.6.0"
  }
}
